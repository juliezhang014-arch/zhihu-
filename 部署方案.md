# 「图片模板合成器」站内上线部署方案

| 项目 | 内容 |
| --- | --- |
| 产品名称 | 图片模板合成器 |
| 线上地址 | https://poster.zhihuredeem.top |
| 代码仓库 | GitHub `juliezhang014-arch/zhihu-`（私有） |
| 部署平台 | Vercel（Serverless，函数区域 sin1 新加坡） |
| 数据存储 | Upstash Redis（生产）/ 本地 JSON 文件（开发） |
| 文档版本 | v1.0（2026-08-19） |

---

## 一、功能介绍

### 1.1 产品定位与主要功能

「图片模板合成器」是一个**图片模板文字合成工具**：站方（管理员）预先设计好带文字框位/图片位布局的图片模板并发布；访客无需登录、无需设计能力，选择模板后填入文字（或从图库选择图片），即可在浏览器内实时预览并一键导出合成图（高清 JPEG），用于配图、分享等场景。

核心能力：

1. **模板化合成**：内置模板 + 管理员 DIY 模板，文字/图片按模板预设版式精确落到指定位置。
2. **实时预览**：所有编辑操作在 Canvas 画布上毫秒级实时反馈，所见即所得。
3. **高清导出**：3 倍分辨率离屏渲染，导出 JPEG（照片类模板约 3~5MB）。
4. **可视化 DIY 模板工坊**：管理员在画布上自由拖框、8 向缩放摆放文字位与图片位，无需写代码即可发布新模板。
5. **图片位玩法**：模板可预置图片选项库（外链 URL 或上传图库），访客点击图片位逐位单选嵌入（如「给电影排序」类玩法）。
6. **多管理员权限体系**：超级管理员 / 高级管理员 / 普通管理员三级角色，支持按模板粒度授权。
7. **移动端适配**：手机端提供悬浮实时预览 + 底部快捷编辑抽屉。

### 1.2 用户路径

**访客路径（免登录）**

```
打开页面 → 模板列表加载（内置 + 已发布 DIY，~10KB 秒开）
   → 下拉选择模板（背景/图片素材已后台预取）
   → 编辑文字（逐框填写，或点图片位从图库选图）
   → 画布实时预览（可调整字体/颜色/字号/对齐/阴影）
   → 点击「生成图片」→ 预览弹窗 → 下载 / 复制到剪贴板 → 完成
```

**管理员路径**

```
打开页面 → 顶部「管理后台」→ 登录（HMAC 令牌，7 天有效）
   ├─ 模板库页（默认）：发布/下架、删除、拖拽排序、隐藏内置模板、一键进入工坊编辑
   ├─ DIY 工坊页：选基础模板克隆 → 画布添加文字框/图片位（拖拽 + 8 向缩放）
   │        → 编辑样式 → 上传图片选项库（本地压缩 / 外链 URL）→ 保存并发布
   └─ 权限管理：查看/添加管理员、设定角色、按管理员开放指定模板、按模板分配编辑者
```

### 1.3 功能模块（逐板块说明）

#### A. 用户端板块

| 板块 | 说明 |
| --- | --- |
| ① 顶部导航栏 | 品牌标题「图片模板合成器」；模板下拉选择器（加载中状态提示「模板加载中…」）；「临时换图」上传本地背景图；「重置示例」一键还原默认文案；「管理后台」入口按钮 |
| ② 画布实时预览 | 桌面端左侧粘性画布；实时渲染当前模板背景 + 全部文字框/图片位；选中框显示边框与手柄，支持拖拽移动、8 向缩放、锁定/删除 |
| ③ 文字编辑面板 | 逐框编辑文字内容；全局样式：6 款中文字体、9 个预设颜色、字号缩放、对齐方式、加粗、文字阴影；方向键微调选中框位置 |
| ④ 图片位与图片库 | 模板含图片位时，点击图片位弹出图片库弹窗：网格展示管理员预置选项（外链直连 / 图库存储），逐位单选、可清除选择；外链失效显示「加载失败」角标，不阻断其他位置渲染 |
| ⑤ 临时换图 | 上传任意本地图片作为临时背景（仅内存，不落盘），立即生成新画布继续编辑 |
| ⑥ 生成与导出 | 「生成图片」以 3 倍分辨率离屏渲染 → JPEG 0.92 导出 → 预览弹窗支持「下载图片」与「复制到剪贴板」 |
| ⑦ 移动端适配 | 悬浮实时预览窗（随滚动跟随）；底部快捷编辑抽屉（改文字/选图/开全量样式）；顶栏移动端按钮组 |

#### B. 管理端板块

| 板块 | 说明 |
| --- | --- |
| ① 管理员登录 | 账号密码登录，签发 HMAC-SHA256 签名令牌（有效期 7 天 + 密码版本号 pv，服务端无状态）；登录态持久化于 localStorage；本人可在后台「修改密码」（scrypt 哈希存储，改密后全部旧会话失效，需重新登录） |
| ② 模板库管理 | 全部模板列表（内置 + DIY，含缩略图、插槽数、发布时间）；发布/下架开关；拖拽排序（决定用户端下拉顺序）；隐藏内置模板；删除模板（级联清理背景/图片素材 key）；「载入工坊」一键进入编辑 |
| ③ DIY 模板工坊 | 画布可视化编辑器：添加/删除文字框与图片位、拖拽定位、8 向缩放、锁定；逐框样式编辑（字体/颜色/字号/对齐）；模板基础信息（名称/类别/描述）；保存即发布 |
| ④ 图片选项库 | 支持双来源：本地上传（canvas 压缩至最长边 800px、≤400KB/张、每模板 ≤10 张，base64 存 Redis 独立 key，**不内嵌模板 JSON**）与外链 URL（零存储）；选项缩略图网格、内联改名、删除时同步清空引用 |
| ⑤ 权限管理 | 三级角色：**超管**（全局最高：建号/授权/任意模板）/ **高级管理员**（可编辑、发布、下架他人模板）/ **普通管理员**（默认仅本人模板）；「指定开放模板」按管理员授权指定模板；「分配编辑者」按模板指定可编辑管理员；支持直接添加管理员账号并设定初始角色；超管可一键**重置任意管理员密码**（自定义新密码）或**初始化密码为默认值 admin123**（登录前忘记密码兜底，两种操作均使目标账号会话全部失效） |
| ⑥ 保存保护 | 保存失败（如登录态失效 401）时明确报错，不再静默丢失；模板校验（至少一个文字框或图片位；有图片位则图库不能为空） |

#### C. 后端 API 板块（Express on Vercel Serverless）

| 分组 | 路由 | 鉴权 | 说明 |
| --- | --- | --- | --- |
| 管理员 | `POST /api/admin/login` | 公开 | 登录，签发 HMAC 令牌 |
| 管理员 | `POST /api/admin/register` | 超管 | 注册新管理员 |
| 管理员 | `GET /api/admin/users` | 登录 | 管理员列表 |
| 管理员 | `POST /api/admin/users/update-role` | 超管 | 修改角色/权限 |
| 管理员 | `POST /api/admin/users/assign-templates` | 超管 | 按管理员开放指定模板 |
| 管理员 | `POST /api/admin/users/create` | 超管 | 直接添加管理员 |
| 管理员 | `DELETE /api/admin/users/:username` | 超管 | 删除管理员 |
| 管理员 | `POST /api/admin/change-password` | 登录 | 本人修改密码（旧密码校验 + 新密码 scrypt 哈希；改密后旧会话全部失效） |
| 管理员 | `POST /api/admin/users/reset-password` | 超管 | 重置他人密码为自定义新密码（忘记密码兜底；目标账号会话全部失效） |
| 管理员 | `POST /api/admin/users/reset-password-default` | 超管 | 一键初始化他人密码为默认值 admin123（登录前忘记密码兜底；目标账号会话全部失效） |
| 模板 | `GET /api/templates` | 公开 | 模板列表（内置 + DIY，gzip ~10KB，边缘缓存 60s） |
| 模板 | `POST /api/templates` | 登录 | 新建/更新 DIY 模板（背景图剥离存储） |
| 模板 | `DELETE /api/templates/:id` | 登录 | 删除模板（级联清理素材） |
| 模板 | `POST /api/template-order` | 登录 | 保存模板排序 |
| 模板 | `POST /api/templates/builtin-state` | 登录 | 隐藏/显示内置模板 |
| 模板 | `POST /api/templates/assign-editors` | 登录 | 模板分配编辑者 |
| 素材 | `GET /api/templates/:id/images` | 公开 | 模板图片选项 dataUrl 集合 |
| 素材 | `GET /api/templates/:id/bg` | 公开 | 模板背景图 dataUrl（按需拉取） |
| 素材 | `POST /api/templates/:id/images` | 登录 | 上传/删除图片选项（分块 ≤1.5MB） |
| AI | `POST /api/generate-review` | 公开 | AI 一键生成锐评文案（需 GEMINI_API_KEY，前端入口待接入） |

#### D. 存储与基础设施板块

- **存储抽象**：`api/_storage.ts` 双模式——生产走 Upstash Redis（KEY_PREFIX `zhihu-poster:`），本地开发走 `data_storage/*.json`（大体积 base64 素材用独立 `.raw` 文件）。
- **数据隔离**：模板 JSON 仅含元数据；背景图与上传图片选项的 base64 存**独立 key**（`bg:<模板id>`、`image:<模板id>:<选项id>`），按需拉取——模板列表从 4.75MB 降到 ~10KB。
- **传输与缓存**：服务端 zlib gzip 压缩（>1KB 响应）；模板列表边缘缓存 `s-maxage=60 + SWR 300`；背景/图片端点浏览器 `max-age=60` + 边缘 `s-maxage=300`。
- **性能**：函数区域 sin1（新加坡，贴近站内用户）；首屏后台预取全部照片模板的背景与图片素材（切换即渲染）；导出 JPEG 0.92（20MB→3~5MB）。
- **部署**：GitHub push → Vercel 自动构建部署（`npm run build` 生成 `api/index.js` 单文件函数 + 前端静态资源）；`vercel.json` 将 `/api/*` 全部重写至函数入口，函数时长上限 60s。

---

## 二、页面与代码结构

### 2.1 页面结构

单页应用（SPA），两个视图：

| 视图 | 路由 | 说明 |
| --- | --- | --- |
| 用户端合成页 | `/` | 顶栏 + 画布 + 编辑面板 + 各类弹窗/抽屉（图片库、预览、登录、移动端抽屉） |
| 管理后台 | 同页内视图切换 | 登录后进入，含「模板库」与「DIY 工坊」两个页签 + 权限管理弹窗 |

### 2.2 代码目录结构

```
图片模板/
├── index.html                    # 页面入口（标题、Google Fonts 引入）
├── package.json                  # 依赖与脚本（dev/build/start/lint）
├── vite.config.ts                # 前端构建配置
├── tsconfig.json
├── vercel.json                   # Vercel 配置：/api 重写、函数区域 sin1、时长 60s
├── server.ts                     # 本地开发服务器（Express + Vite，端口 3000）
├── api/                          # 后端源码（Vercel 函数只打包此目录）
│   ├── _handler.ts               # Serverless 函数入口（挂载 Express 应用）
│   ├── _app.ts                   # Express 应用：全部 20 条路由 + 鉴权 + gzip 中间件
│   ├── _templates.ts             # 模板数据/排序/内置覆盖辅助
│   ├── _storage.ts               # 存储抽象（Upstash Redis / 本地 JSON 双模式）
│   └── index.js                  # esbuild 预打包产物（npm run build 生成，随代码提交）
└── src/                          # 前端源码
    ├── main.tsx                  # React 入口
    ├── App.tsx                   # 根组件：状态管理、数据拉取、后台预取、模块组装
    ├── index.css                 # 全局样式（Tailwind 4）
    ├── types.ts                  # 类型定义（Template/TextSlot/ImageSlot/ImageOption/AdminUser）
    ├── data/                     # 静态配置
    │   ├── templates.ts          # 内置模板（「民间锐评人·请就位」）
    │   ├── fonts.ts              # 6 款中文字体配置
    │   └── colors.ts             # 9 个预设颜色
    ├── services/
    │   └── api.ts                # 全部后端接口的前端封装（含登录态、分块上传）
    ├── utils/
    │   ├── canvasRenderer.ts     # Canvas 渲染引擎（背景/文字/图片位 cover-fit/占位兜底）
    │   └── imageCompress.ts      # 图片压缩（≤800px、≤400KB，参考兄弟项目实现）
    └── components/               # 12 个组件
        ├── Header.tsx                    # 顶栏：模板选择/临时换图/重置/后台入口
        ├── TemplateCanvas.tsx            # 画布预览 + 拖拽/8 向缩放交互
        ├── EditorPanel.tsx               # 文字编辑面板（字体/颜色/样式）
        ├── ImageLibraryModal.tsx         # 图片库弹窗（逐位单选）
        ├── PreviewModal.tsx              # 生成结果弹窗（下载/复制）
        ├── MobileFloatingPreview.tsx     # 移动端悬浮实时预览
        ├── MobileQuickEditor.tsx         # 移动端快捷编辑抽屉
        ├── AdminLoginModal.tsx           # 管理员登录弹窗
        ├── ChangePasswordModal.tsx       # 修改密码弹窗（旧密码校验 → 新密码哈希）
        ├── AdminPanel.tsx                # 管理后台主面板（模板库 + DIY 工坊，最大组件）
        ├── AdminPermissionsModal.tsx     # 权限管理（角色/指定开放模板/添加管理员）
        └── TemplateEditorAssignModal.tsx # 模板分配编辑者
```

### 2.3 技术栈

| 层 | 技术 | 说明 |
| --- | --- | --- |
| 前端 | React 19 + TypeScript + Vite 6 | SPA，Canvas 离屏渲染导出 |
| 样式 | Tailwind CSS 4 + lucide-react 图标 + motion 动画 | 全开源 |
| 后端 | Express 4（Vercel Serverless，esbuild 预打包单文件） | 20 条 REST 路由 |
| 存储 | Upstash Redis（生产）/ 本地 JSON（开发） | 双模式抽象 |
| 密码存储 | Node 内置 scrypt（N=16384, r=8, p=1，参数内嵌格式） | 零新增依赖；历史明文账号登录时惰性迁移 |
| 鉴权 | HMAC-SHA256 签名令牌（7 天 TTL + 密码版本号 pv）+ ADMIN_TOKEN_SECRET | 服务端无状态；改密/重置即踢旧会话 |
| AI（可选） | Google Gemini（@google/genai） | 仅 `generate-review` 端点使用 |
| 部署 | GitHub + Vercel | push 自动构建，函数区域 sin1 |

### 2.4 核心数据模型

| 模型 | 关键字段 |
| --- | --- |
| Template | id、name、category、description、width/height、bgType（`image` 照片 / `vector` 内置矢量底图）、bgImageUrl（元数据仅存空串或外链，base64 走独立 key）、slots、imageOptions、isPublished、isBuiltin |
| TextSlot | id、label、value、x/y/width/height（百分比坐标）、fontSize、align、tagBgColor/tagTextColor、locked、type:'text' |
| ImageSlot | type:'image'、id、label、x/y/width/height、value（选中的 optionId）、locked |
| ImageOption | id、label、source（`url` 外链 / `upload` 图库）、url（仅外链有） |
| AdminUser | username、role（super_admin/senior_admin/admin）、permissions（canEditOthers/canPublishOthers/canDeleteOthers/allowedTemplateIds）、pv（密码版本号，改密即失效旧令牌） |

---

## 三、已实现

以下功能均已开发完成并上线验证：

- ✅ 访客端全流程：模板选择 → 文字编辑（6 字体/9 色/字号/对齐/加粗/阴影）→ 画布实时预览（拖拽、8 向缩放、锁定）→ 图片位逐位单选 → JPEG 高清导出（下载/复制）
- ✅ 内置模板「民间锐评人·请就位」（6 个文字位）；管理员已通过工坊发布 3 个 DIY 模板（含照片背景模板与图片位模板）
- ✅ 管理端：登录、模板库（发布/下架/删除/排序/隐藏内置）、DIY 工坊（画布拖框 + 样式面板）、图片选项库（上传压缩存储/外链）
- ✅ 三级管理员权限体系：建号、角色调整、按管理员开放指定模板、按模板分配编辑者
- ✅ 账号安全：密码 scrypt 哈希存储（历史明文账号登录时自动惰性迁移）；本人「修改密码」入口 + 超管「重置他人密码」（自定义新密码）与「初始化密码为默认值 admin123」入口；改密/重置后该账号全部旧会话立即失效
- ✅ 后端 20 条 API + 双模式存储（Redis/JSON）+ 素材独立 key 存储与级联清理
- ✅ 性能优化：模板列表 4.75MB→~10KB（背景剥离 + gzip）、函数区域 sin1、边缘缓存、首屏后台预取（切换模板背景秒渲染）、加载中提示
- ✅ 移动端：悬浮预览 + 快捷编辑抽屉 + 顶栏按钮组
- ✅ 部署链路：GitHub push → Vercel 自动部署（含 `api/index.js` 构建产物提交）
- ✅ 品牌：首页与浏览器外显标题均为「图片模板合成器」

---

## 四、预留待接入

以下能力**接口/占位已就绪，但前端入口未接入**，可按需逐步启用：

| 项 | 现状 | 接入方式 |
| --- | --- | --- |
| AI 一键生成锐评文案 | 后端 `POST /api/generate-review` 已实现（Gemini 2.5 Flash，JSON 输出映射各文字位），前端无调用入口 | 在编辑面板加「AI 生成」按钮 + 配置 `GEMINI_API_KEY` 环境变量即可 |
| 更多模板位 | 用户端下拉已有「更多模板持续更新中…」占位 | 管理员在工坊发布新模板即自动填充 |
| 模板分配编辑者 | 后端与弹窗组件已实现 | 超管在模板库中对模板执行「分配编辑者」即可使用 |
| 字体扩展 | 字体配置为数据驱动（`data/fonts.ts`） | 新增字体只需加配置项 + index.html 引入字体文件 |

---

## 五、未包含

以下功能**不在本期范围**（无代码预留），供评审确认边界：

- 访客账号体系：用户端纯免登录，无注册/登录/个人中心
- 统计与数据分析：无访问量、模板使用量、导出量埋点
- 内容互动：无点赞、评论、分享统计
- **站内人工内容审核流**：管理员发布模板即对外可见，无「保存草稿 → 审核 → 发布」流程（建议由站内运营流程补充，或后续在工坊增加草稿态）
- 上传素材自动合规检测：图片内容靠人工把关，无涉黄涉政自动识别
- 多语言、站内搜索、模板分类页、付费/会员体系
- 素材 CDN 化：背景与图库素材存 Redis，未接入对象存储/CDN

---

## 六、代码来源说明

1. **项目初始化**：基于 Vercel React 示例脚手架（package.json 工程名 `react-example`）初始化，此后全部业务代码均为本项目开发。
2. **开发方式**：本地工作区「Vibe Coding」目录下的独立项目，采用 AI 辅助编程（Claude Code）迭代开发，18 次提交完整记录于 Git 历史（GitHub 私有仓库 `juliezhang014-arch/zhihu-`）；所有业务逻辑、页面、接口均为自主开发，非第三方成品套壳。
3. **跨项目复用**：`src/utils/imageCompress.ts` 的图片压缩实现参考了同工作区兄弟项目「票码下发」的上传压缩模式（缩小至最长边 800px、白底填充、JPEG 降质兜底），仅参考实现思路，代码独立编写。
4. **开源依赖**（均为开源许可）：React 19、Vite 6、TypeScript、Tailwind CSS 4、Express 4、@upstash/redis、lucide-react（图标）、motion（动画）、esbuild/tsx（构建工具链）、@google/genai（可选 AI 功能）。
5. **字体**：Google Fonts 开源中文字体（Noto Sans SC、Noto Serif SC、站酷快乐体、知命行书、马善政、龙藏）。
6. **模板素材**：内置模板「民间锐评人·请就位」为项目自制设计；已发布照片模板的底图与图片选项为管理员上传素材。
7. **外部接口**：仅可选 AI 文案功能调用 Google Gemini；核心合成流程 100% 本地浏览器渲染，不依赖任何外部服务。

---

## 七、审核预览方式

### 7.1 线上预览（推荐）

**访客路径**：直接访问 https://poster.zhihuredeem.top ，可体验模板选择、文字编辑、图片位选图、生成导出全流程（无需账号）。

**管理后台路径**：

1. 访问线上地址，点击顶部「管理后台」按钮；
2. 使用超管账号登录（账号由项目负责人保管；如需为审核人员单独开通，可由超管登录后在「权限管理 → 添加管理员」中直接创建账号并设定角色，无需改代码）；
3. 进入后可体验：模板库管理（发布/下架/排序/隐藏内置）、「载入工坊」编辑既有模板、DIY 工坊新建模板（拖框 + 图片选项库上传/外链）、权限管理。

### 7.2 本地运行

```bash
git clone https://github.com/juliezhang014-arch/zhihu- && cd 图片模板
npm install
npm run dev          # 启动本地开发服务器
# 浏览器打开 http://localhost:3000
```

本地默认管理员账号：`zhangxiyu` / `123456`（超管）、`admin` / `admin123`（普通管理员）。
本地数据存于 `data_storage/` JSON 文件，与生产 Redis 完全隔离。

### 7.3 接口直连验证

```bash
# 模板列表（含缓存头）
curl -D - https://poster.zhihuredeem.top/api/templates | head -20

# 模板背景图 dataUrl（200 + 2.37MB 即正常）
curl -o /dev/null -w "%{http_code} %{size_download}B\n" \
  https://poster.zhihuredeem.top/api/templates/diy-1786895964812/bg

# 图片选项集合
curl https://poster.zhihuredeem.top/api/templates/diy-1786895964812/images
```

### 7.4 生产环境变量（Vercel 后台已配置）

| 变量 | 用途 | 必填 |
| --- | --- | --- |
| `ADMIN_TOKEN_SECRET` | 管理员令牌 HMAC 签名密钥（Sensitive） | 是 |
| `UPSTASH_REDIS_REST_URL` / `UPSTASH_REDIS_REST_TOKEN` | Upstash Redis 连接 | 是 |
| `GEMINI_API_KEY` | AI 一键锐评（启用四、①时配置） | 否 |

### 7.5 部署链路

```
git push origin main → Vercel 自动构建（vite build + esbuild 打包 api/index.js）
   → 自动发布生产环境（函数区域 sin1，时长上限 60s）
```

- 构建脚本见 `package.json` 的 `build` 命令；`api/index.js` 为构建产物随代码提交（改后端必须重新 build）。
- 发布为原子切换，无停机窗口；回滚可在 Vercel 后台一键回到上一版本。

### 7.6 审核重点建议

| 审核项 | 验证点 |
| --- | --- |
| 权限隔离 | 普通管理员无法编辑/发布他人模板；未登录无法调用写接口（401） |
| 上传约束 | 图片选项 ≤400KB/张、每模板 ≤10 张；后端二次校验并拒绝超限 |
| 数据边界 | 模板列表响应 ~10KB（背景/素材不内嵌）；localStorage 仅存登录态，无图片 base64 |
| 性能 | 首屏模板列表秒开；切换照片模板背景即时渲染；导出图 3~5MB |
| 账号安全 | 密码 scrypt 哈希存储（非明文）；改密/被重置后旧会话立即失效（令牌 pv 校验）；未登录无法调用任何写接口（401） |

---

*本文档随代码仓库维护；功能变更后以代码与线上实际表现为准。*
